```
╔═══════════════════════════════════════════════════════════════════════════════╗
║                    NETWORK CONNECTIVITY SERVICE ARCHITECTURE                   ║
╚═══════════════════════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────────────────────┐
│                              UI LAYER (Compose)                                │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                                │
│  ┌─────────────────┐  ┌──────────────────────┐  ┌─────────────────────────┐ │
│  │ OfflineScreen   │  │ NetworkAwareContent  │  │   HomeScreen            │ │
│  │                 │  │                      │  │   (Example)             │ │
│  │ - Animations    │  │ - onlineContent      │  │                         │ │
│  │ - Refresh btn   │  │ - offlineContent     │  │ - Uses NetworkAware     │ │
│  │ - State display │  │ - loadingContent     │  │ - Shows online/offline  │ │
│  └────────┬────────┘  └──────────┬───────────┘  └────────────┬────────────┘ │
│           │                      │                            │              │
│           │ observes             │ uses                       │ uses         │
│           ▼                      ▼                            ▼              │
└───────────────────────────────────────────────────────────────────────────────┘
            │                      │                            │
            └──────────────────────┴────────────────────────────┘
                                   │
                                   ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                           VIEWMODEL LAYER (MVVM)                               │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                                │
│                         ┌───────────────────────┐                             │
│                         │  OfflineViewModel     │                             │
│                         │                       │                             │
│                         │  + networkState       │   StateFlow<NetworkState>   │
│                         │  + checkNetworkState()│   (Reactive Updates)        │
│                         │                       │                             │
│                         └──────────┬────────────┘                             │
│                                    │                                           │
│                                    │ uses                                      │
│                                    ▼                                           │
└───────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                          DOMAIN LAYER (Models)                                 │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                                │
│                      ┌────────────────────────┐                               │
│                      │   NetworkState         │   (Sealed Class)              │
│                      │   (sealed class)       │                               │
│                      ├────────────────────────┤                               │
│                      │ • Connected            │   ✓ Device online             │
│                      │ • Disconnected         │   ✗ Device offline            │
│                      │ • Checking             │   ⟳ Verifying connection      │
│                      └────────────────────────┘                               │
│                                                                                │
└───────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ emitted by
                                    ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                      SERVICE/DATA LAYER (Platform)                             │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                                │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │           NetworkConnectivityObserver (Interface)                       │ │
│  │                                                                         │ │
│  │  + observe(): Flow<NetworkState>    // Real-time monitoring            │ │
│  │  + isConnected(): Boolean            // Instant check                  │ │
│  └─────────────────────────────┬───────────────────────────────────────────┘ │
│                                │                                              │
│                                │ implemented by                               │
│                                ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │     AndroidNetworkConnectivityObserver (Implementation)                 │ │
│  │                                                                         │ │
│  │  • Uses Android ConnectivityManager                                     │ │
│  │  • Registers NetworkCallback                                            │ │
│  │  • Emits NetworkState via Flow                                          │ │
│  │  • Auto-detects changes (WiFi, Mobile, Airplane Mode)                   │ │
│  │  • Battery-efficient (event-based, not polling)                         │ │
│  └─────────────────────────────┬───────────────────────────────────────────┘ │
│                                │                                              │
│                                │ uses                                         │
│                                ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │              Android Platform APIs                                      │ │
│  │                                                                         │ │
│  │  • ConnectivityManager                                                  │ │
│  │  • NetworkCapabilities                                                  │ │
│  │  • NetworkCallback                                                      │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                                │
└───────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────────┐
│                    DEPENDENCY INJECTION (Koin)                                 │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                                │
│  CoreModule:                                                                   │
│    single<NetworkConnectivityObserver> {                                      │
│      AndroidNetworkConnectivityObserver(androidContext())                     │
│    }                                                                           │
│                                                                                │
│  AppModule:                                                                    │
│    viewModel { OfflineViewModel(get()) }                                      │
│                                                                                │
└───────────────────────────────────────────────────────────────────────────────┘

╔═══════════════════════════════════════════════════════════════════════════════╗
║                             DATA FLOW                                          ║
╚═══════════════════════════════════════════════════════════════════════════════╝

    Network Change (WiFi/Mobile/Airplane Mode)
                   │
                   ▼
    Android OS ConnectivityManager detects change
                   │
                   ▼
    NetworkCallback.onAvailable() / onLost()
                   │
                   ▼
    AndroidNetworkConnectivityObserver emits NetworkState
                   │
                   ▼
    Flow<NetworkState> updates
                   │
                   ▼
    ViewModel collects and exposes as StateFlow
                   │
                   ▼
    UI observes StateFlow with collectAsState()
                   │
                   ▼
    UI re-composes automatically with new state
                   │
                   ▼
    User sees updated UI (Offline/Online/Checking)


╔═══════════════════════════════════════════════════════════════════════════════╗
║                          USAGE PATTERNS                                        ║
╚═══════════════════════════════════════════════════════════════════════════════╝

Pattern 1: Dedicated Screen
┌──────────────────┐
│ Navigate to      │
│ OfflineScreen    │──→  Shows full-screen offline UI with refresh
└──────────────────┘

Pattern 2: Conditional Rendering
┌──────────────────┐
│ NetworkAware     │──→  Shows different content based on state
│ Content          │     (online/offline/loading)
└──────────────────┘

Pattern 3: ViewModel Integration
┌──────────────────┐
│ Inject           │──→  Use in business logic, API calls,
│ NetworkObserver  │     data loading decisions
└──────────────────┘

Pattern 4: Inline Check
┌──────────────────┐
│ isOnline()       │──→  Simple boolean check in composables
└──────────────────┘


╔═══════════════════════════════════════════════════════════════════════════════╗
║                      FEATURES & BENEFITS                                       ║
╚═══════════════════════════════════════════════════════════════════════════════╝

✓ Real-time Updates       → No manual refresh needed
✓ Battery Efficient       → Event-based, not polling
✓ Type Safe               → Sealed classes prevent bugs
✓ Testable                → Interface-based design
✓ Reusable                → Works across entire app
✓ MVVM Architecture       → Clean separation of concerns
✓ Dependency Injection    → Easy to inject anywhere
✓ Material 3 UI           → Modern, beautiful design
✓ Animations              → Smooth transitions
✓ Well Documented         → Complete guides included


╔═══════════════════════════════════════════════════════════════════════════════╗
║                         FILE STRUCTURE                                         ║
╚═══════════════════════════════════════════════════════════════════════════════╝

app/src/main/java/com/zonkesoft/fooball/
│
├── domain/
│   └── NetworkState.kt                   ← Sealed class (Connected/Disconnected/Checking)
│
├── core/utils/
│   └── NetworkConnectivityObserver.kt    ← Interface + Android implementation
│
├── ui/
│   ├── viewmodel/
│   │   └── OfflineViewModel.kt           ← State management + refresh logic
│   │
│   ├── screens/
│   │   ├── OfflineScreen.kt              ← Full-screen offline UI
│   │   └── HomeScreen.kt                 ← Updated with example
│   │
│   └── components/
│       └── NetworkAwareContent.kt        ← Reusable helpers
│
└── di/
    ├── CoreModule.kt                     ← Service registration
    └── AppModules.kt                     ← ViewModel registration


Documentation Files:
├── QUICK_START_GUIDE.md                  ← How to use right now
├── NETWORK_SERVICE_DOCUMENTATION.md      ← Complete reference
└── IMPLEMENTATION_SUMMARY.md             ← What was built

```
